#include <sys/typees.h>
#include <sys/stat.h>
#include <fcntl.h>
#include <stdio.h>

int main() {
    int fd;
    int buf;
    fd = open("/dev/ex_test", 0_RDONLY);
    for (i=0;i<5;i++) {
        read(fd,buf,sizeof(buf));
        printf("Input: >>> %d <<<\n",buf);
    }
}
